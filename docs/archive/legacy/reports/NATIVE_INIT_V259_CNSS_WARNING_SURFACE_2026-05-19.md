# Native Init V259 CNSS Warning Surface Report

## Summary

- Status: PASS
- Tool: `scripts/revalidation/wifi_cnss_warning_surface_probe.py`
- Output: `tmp/wifi/v259-cnss-warning-surface/`
- Decision: `cnss-warning-surface-classified`
- Device command type: read-only only
- Daemon execution: none

## Purpose

V258 reduced the V257 live transcript and found three non-fatal warnings:

- `perfd-client-unavailable`
- `kmsg-write-denied`
- `shell-quote-noise`

V259 classifies these warnings without running `cnss-daemon` again.

## Validation

Static:

```text
python3 -m py_compile scripts/revalidation/wifi_cnss_warning_surface_probe.py
git diff --check
```

Functional:

```text
python3 scripts/revalidation/wifi_cnss_warning_surface_probe.py
python3 scripts/revalidation/wifi_cnss_warning_surface_probe.py --out-dir tmp/wifi/v259-cnss-warning-surface-verify
```

Result:

```text
decision: cnss-warning-surface-classified
pass: True
```

Critical checks:

```text
v258-prerequisite: PASS
cnss-daemon-absent: PASS
```

Warning checks:

```text
dev-kmsg-observed: FAIL/WARN, /dev/kmsg absent in native root
property-surface-observed: FAIL/WARN, socket=False area=False
perfd-surface-classified: PASS/WARN, binary=False socket=False
helper-kmsg-source-clean: PASS/WARN, helper_source_contains_kmsg=False
```

## Findings

### perfd

```text
perfd-client-surface-present-socket-absent / android-service-gap-warning
```

Evidence:

```text
/mnt/system/system/system_ext/lib64/libqti-perfd-client_system.so
/mnt/system/system/system_ext/lib/libqti-perfd-client_system.so
/dev/socket/perfd: absent
/mnt/system/vendor/bin/perfd: absent
```

Interpretation:

- CNSS can print `Failed to become a perfd client` because a perfd client surface exists, but no perfd runtime service/socket exists in native init.
- This did not block V257 start-only lifecycle.

### property service

```text
property-service-socket-absent / android-service-gap-warning
property-area-absent / android-runtime-gap-warning
```

Evidence:

```text
/dev/socket/property_service: absent
/dev/__properties__: absent
```

Interpretation:

- Native init does not provide Android property service or property area.
- V257 start-only still passed, so this is not a start-only blocker, but it remains relevant for broader Wi-Fi service behavior.

### kmsg and shell quote noise

```text
shell-quote-noise-not-helper-source / daemon-or-library-stderr
```

Evidence:

```text
/dev/kmsg: absent in native root
helper_source_contains_kmsg=False
```

Interpretation:

- The V257 `/dev/kmsg` denial and shell quote noise are not generated by `a90_android_execns_probe.c` source.
- The noise is best treated as daemon/library logging-path stderr until proven otherwise.

## Decision

- V259 classifies V258 warnings as Android runtime service/logging gaps, not helper cleanup bugs.
- No daemon remained after validation.
- This still does not authorize Wi-Fi scan/connect/link-up.
- Next work should inspect QRTR/QMI endpoint behavior or design a no-start property/perfd shim model before broader live operations.
